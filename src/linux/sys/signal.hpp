#pragma once

#include "../linux_types.hpp"
// TODO: implement signal
#include <signal.h>

namespace micron
{
/*
union sigval {
  int sival_int;
  void *sival_ptr;
};

struct siginfo_t {
  int si_signo;
  int si_code;
  posix::pid_t si_pid;
  posix::uid_t si_uid;
  void *si_addr;
  int si_status;
  union sigval si_value;
};
*/
};
